#include <stdio.h>
#include <string.h>
int main()
{
    char s[]="hello",st[]="aorld";
    printf("%d",strcmp(s,st));

    // strcpy(str,strcat(s,st));
    // printf("%s",str);
    //scanf("",&);
    //printf("");
    return 0;
}